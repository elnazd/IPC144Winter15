#include<stdio.h>
#include<string.h>

#define SIZE 30
int main()
{
	//step1
	char input[SIZE+1];
	//step2
	printf("Please input a string:");
	scanf("%30[^\n]",input);

	//step3
	int length = strlen(input);
	printf("The length of the string is %d",length);
	
	return 0;

}
